import { el } from "./util.js";

/* Temporary stub. Task 13 writes the real checkride - the strip on the index,
   the render and the teardown. Kept minimal so main.js and home.js have
   something real to import against until then. */
export const checkrideStrip = () => el("span");
export function renderCheckride() {}
export function stopCheckride() {}
